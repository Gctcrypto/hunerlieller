# Nixpacks — Hünerli Eller statik site (çok sayfalı)
# Coolify Build Pack: Nixpacks seçildiğinde kullanılır.
# NOT: -s (SPA modu) OLMADAN çalışır — çok sayfalı HTML sitesi

providers = ["node"]

[phases.install]
cmds = []

[start]
cmd = "npx serve . -l ${PORT:-3000} --no-clipboard"
