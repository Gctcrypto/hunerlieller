# Nixpacks yapılandırması — Hünerli Eller statik site
# Coolify'de Build Pack olarak "Nixpacks" seçildiğinde bu dosya kullanılır.
# Docker tercih edilirse Dockerfile otomatik algılanır.

providers = ["node"]

[phases.install]
cmds = []

[start]
cmd = "npx serve -s . -l ${PORT:-3000} --no-clipboard"
