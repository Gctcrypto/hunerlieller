{
  "name": "hunerlieller",
  "version": "1.0.0",
  "description": "Hünerli Eller — Kırıkkale Delice Ev Yemekleri statik web sitesi",
  "private": true,
  "scripts": {
    "start": "npx serve . -l ${PORT:-3000} --no-clipboard",
    "dev": "npx serve . -l 3000 --no-clipboard"
  },
  "engines": {
    "node": ">=18"
  }
}
